# 사전등록 — #4 `taker_imb_30` 강건성 배터리

**작성 시각: 배터리를 하나도 돌리기 전.** 커밋 후 실행한다.

## 무엇이 나왔나

`#3` 에서 **54 시행 중 처음으로 관문 6/7 을 통과**했다.

```
taker_imb_30   Sharpe +1.55 · 연복리 +47.7% · 낙폭 20.0%
  ① 시점정합 OK   ② 표본길이 OK   ③ 청산 OK
  ④ 벤치대비 +0.65 OK   ⑤ 비용3배 +0.56 OK   ⑥ 낙폭 OK
  ⑦ 잡음바닥 **NG** — 바닥 +1.80 vs 관측 +1.55
```

신호는 **테이커 매수 비중** `sum(taker_buy_quote, 30일) / sum(quote_volume, 30일)`.
높은 쪽 롱, 낮은 쪽 숏. 이건 가격에서 나오지 않는 **주문흐름** 정보다.

## 이미 한 진단 (버그 사냥, 가설검정 아님)

| | 결과 | 뜻 |
|---|---|---|
| 비율이 [0,1] 안인가 | 54.9 만 봉 중 이탈 1 | 정상 |
| 분포 | 평균 0.4870 · 표준편차 0.0215 | **산포가 매우 좁다** |
| 30 일 자기상관 | +0.365 | 지속하지만 고정은 아님 |
| 상위분위 100 일 유지율 | 34.6% | **진짜 회전하는 신호** — 고정 포트폴리오 아님 |
| 횡단면 격차 (상위 20% − 하위 20%) | 0.018 ~ 0.022 | **경제적 내용이 얇다** |

명백한 버그는 안 나왔다. **다만 격차 2%p 로 Sharpe 1.55 가 나온 건
그 자체로 이상하다** — 그래서 배터리를 돌린다.

## ⑦ 에 대한 열린 방법론 문제 — 미리 적어둔다

잡음바닥 공식은 시행이 **독립**이라고 가정한다. 그런데 `#3` 에서 잰
**실효 차원이 49 개 중 8.0** 이었다. 명목 54 건이 실제로는 13 건쯤의
독립 베팅이다.

| 시행수 가정 | 바닥 | 관측 1.55 |
|---|---:|---|
| **명목 54** | **+1.80** | 기각 |
| **실효 13** | **+1.48** | **통과** |

**어느 쪽이 옳은지 나는 모른다.** 그래서 **둘 다 보고하고 어느 쪽도 고르지 않는다.**
이건 과거 데이터로 풀 수 있는 문제가 아니고, **전진검증이 유일한 해결책**이다.

배터리를 통과하든 못 하든 **배포하지 않는다.**

## 배터리 6 종 — **죽일 수만 있고 승격시킬 수 없다**

전부 통과해야 후보로 유지되고 **하나라도 무너지면 기각**이다.
최고 변형을 고르지 않는다. 그래서 새 가설이 아니라 **기존 1 건의 진단**이고
원장에 새로 기록하지 않는다.

| # | 검사 | 기각 조건 |
|---|---|---|
| 1 | **창 길이 고원** — 7·14·30·60·90 일 | 30 만 되고 이웃이 무너지면 과최적화 |
| 2 | **하위표본** — 전반/후반 절반 | 한쪽에서만 되면 표본 특수성 |
| 3 | **비용 민감도** — 3·5·10 배 | 10 배에서 벤치 미달이면 비용에 얹힌 것 |
| 4 | **분위 민감도** — 10·20·30% | 20% 에서만 되면 과최적화 |
| 5 | **자산 절제** — 기여 상위 10 종목 제외 | 소수 종목이 만들었으면 ⑪ |
| 6 | **모멘텀 직교화** — `mom_30` 과의 상관 | 상관 0.8 넘으면 모멘텀의 변장 |

## 미리 선언하는 것

**1. 파라미터를 재탐색하지 않는다.** 30 일은 `#3` 에서 처음 쓴 값이고 그대로 둔다.
고원 검사는 **30 을 바꾸려는 게 아니라 이웃이 살아 있는지** 보는 것이다.

**2. 통과해도 배포하지 않는다.** ⑦ 이 열려 있고 그건 과거로 못 푼다.
통과하면 **전진검증에 사전등록 단일 시도로 올린다.** 판정은 최소 1 년 뒤.

**3. 부호를 뒤집지 않는다.** `taker_imb_7`(+1.08)과 `taker_imb_30`(+1.55)이
둘 다 양수라 방향은 일관된다. `taker_imb_chg`(+0.07)는 무의미했다.

## 예상 — 틀려도 그대로 둔다

**6 종 중 1~2 개에서 무너질 것으로 본다.** 근거: 이 프로젝트와 앞선 두
프로젝트에서 강건성 배터리를 완주한 후보가 **한 번도 없었다.**

가장 위험한 건 **2 번 하위표본**이다. 주문흐름 정보의 예측력은 시장 성숙과
함께 사라지는 종류이고, 2020~2022 의 미성숙 구간이 결과를 만들었을 가능성이 크다.

가장 살아남을 것 같은 건 **1 번 고원**이다. `taker_imb_7` 이 이미 +1.08 이라
이웃이 죽어 있지 않다는 증거가 하나 있다.

**구체적 예상: 전반 Sharpe 2.0 이상 / 후반 0.5 이하로 갈라져 2 번에서 기각.**
