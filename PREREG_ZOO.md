# 사전등록 — #3 전략 동물원

**작성 시각: 하나도 돌리기 전.** 커밋한 뒤에 실행한다.

## 무엇을 하는가

"세상에 공개된 전략을 전부 찾아 검증하고 기록한다."
문헌·깃허브·퀀트 블로그에서 **정확히 명세된** 크립토 횡단면 팩터를 모아
**전부 같은 관문에** 넣는다. 1 차로 29 개, 사냥 결과가 오면 더 붙는다.

## 먼저 — 이건 스스로를 불리하게 만드는 검정이다

잡음바닥은 `평균 + 표준편차 × a_n` 이고 `a_n` 은 시행 수와 함께 자란다.

| 누적 시행 | a_n | 바닥 (평균 0.8 · 표준편차 0.25 가정) |
|---:|---:|---:|
| 5 (현재) | 1.40 | **1.15** |
| **34** (+29) | 2.42 | **1.41** |
| 100 | 2.83 | 1.51 |
| 200 | 3.07 | 1.57 |
| 500 | 3.35 | 1.64 |

**현재 최고 후보가 Sharpe 1.06 이다.** 29 개를 더 태우면 바닥이 1.41 로 올라가
**#2 의 조합이 자동으로 더 확실하게 기각된다.**

이건 버그가 아니라 다중검정의 정의다. 카탈로그를 늘리는 행위 자체가
**남은 후보를 죽인다.** 그걸 알고 시작한다.

그래도 하는 이유 둘:

1. **공간을 지도로 만든다.** 어느 계열이 어디쯤인지 알면 다음에 어디를 팔지가 정해진다
2. **진짜 1.5 짜리가 있다면 그때 걸린다.** 바닥이 올라가도 넘는 놈은 넘는다

## 규칙 — #1 과 완전히 동일하게 고정

```
유니버스   그 시점 거래되던 전 USDⓈ-M 무기한 (상폐 포함)
유동성     직전 30 일 평균 거래대금 상위 50%
포지션     점수 상위 20% 롱 · 하위 20% 숏, 동일가중, 총 그로스 1.0
리밸런싱   7 일
비용       BINANCE_USDM 실측
벤치       BTCUSDT 매수보유
```

**전략마다 다른 것은 점수 하나뿐이다.** 분위·유동성 문턱·리밸런싱을 팩터별로
조정하면 그게 팩터당 파라미터 탐색이고, 시행 수가 29 가 아니라 수백이 된다.
코드 구조(`zoo.Factor`)가 이걸 물리적으로 막는다 — 점수 함수만 갈아끼운다.

## 파라미터 출처

창 길이는 **문헌 표준값**이다. 1·3 일(단기반전), 7·14 일(LTW), 21·30·90 일(모멘텀),
30·90 일(변동성), 60 일(베타·특이변동성), 252 일(52 주 고점).
**결과를 보고 바꾸지 않는다.** 바꾸고 싶어지면 새 사전등록이고 시행 수가 는다.

## 미리 선언하는 것

**1. 29 건 전부 원장에 기록한다.** 기각도 기록한다. 안 하면 벌점이 거짓말이 된다.

**2. 상위 몇 개만 보고하지 않는다.** 전체 분포를 낸다 — 그게 이 검정의 산출물이다.

**3. 통과한 것이 나와도 그 자리에서 배포 후보로 부르지 않는다.**
바닥이 1.41 로 오른 상태에서 넘었다면 그건 의미가 있지만, **하위표본 분할과
전진검증**을 거치기 전엔 후보가 아니다.

**4. 실효 차원을 잰다.** 29 개 전략 수익의 상관행렬로
`실효차원 = n² / ΣΣ c_ij²` 를 계산한다. STONKS-03 에서 25 개 모델이
한국 1.4 · 미국 3.2 차원이었다. **여기서도 낮게 나오면 "더 찾기"는 천장이다.**

**5. 사냥 결과로 팩터를 추가하면 그것도 같은 원장에 들어간다.**
"이번 배치만 따로" 는 없다.

## 예상 — 틀려도 그대로 둔다

**29 개 중 관문 통과 0 건.** 근거:

1. 바닥이 1.41 인데 #1·#2 에서 관측된 최고가 1.06 이었다.
   단일 팩터가 조합(1.06)을 넘을 이유가 별로 없다
2. 크립토 이상현상 49 개 중 2014~2023 재검증 생존이 13 개였다(IRFA 2026).
   생존한 것들도 크기가 줄어 있다
3. 이 프로젝트 계보에서 단일 팩터가 관문을 통과한 적이 한 번도 없다

**구체적 예상:**
- 최고 Sharpe **0.9 ~ 1.2** 사이. 계열은 **위험(저변동성·하방편차·MAX)**
  — STONKS-03 에서 한국·크립토가 둘 다 위험 기반이 상위였다
- **실효 차원 3 ~ 6.** 29 개가 실제로는 대여섯 개 베팅이다
- 모멘텀 계열은 서로 상관 0.8 이상으로 뭉칠 것
- **펀딩 캐리(funding_7/30)가 의외의 상위**일 가능성이 있다.
  #2 에서 시장중립 다리가 펀딩을 받는 게 값이었다

## 반증 조건

- 어떤 팩터가 **바닥 1.41 을 넘고 관문 7 종을 통과**하면 → 진짜다.
  즉시 하위표본 분할 + 전진검증
- 실효 차원이 **10 이상**이면 → 카탈로그에 아직 정보가 남아 있다. 더 찾는다
- 실효 차원이 **5 이하**면 → **"더 찾기"는 천장이다.**
  29 개를 태워 얻은 게 다섯 베팅어치라는 뜻이고,
  그러면 다음 축은 모델 탐색이 아니라 **다른 데이터**(오픈인터레스트·베이시스·현물)다
