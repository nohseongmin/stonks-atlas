# 사전등록 — #5 역변동성 집행 + 신규 팩터 3

**작성 시각: 하나도 돌리기 전.** 커밋 후 실행한다.

## 왜 — 배터리가 가리킨 곳

`#4` 에서 `taker_imb_30` 이 **자산 절제**에서 죽었다.
753 종목 중 10 개를 빼니 Sharpe 1.55 → 0.95. 기여 상위에 **LUNAUSDT**(테라 붕괴)가 있었다.

### 먼저 자기 정정

직전에 "가중치 상한을 건다"고 적었는데 **그건 무의미하다.**
현재 분위 내부가 동일가중이라 각 종목이 정확히 `1/n` 이고 상한이 걸릴 게 없다.
LUNA 는 동일가중을 받았고, 문제는 비중이 아니라 **그 종목의 변동성**이었다.

구조적으로 극단 기여를 막는 것은 **역변동성 가중**이다.

```
분위 내부 비중 ∝ 1 / (직전 30 일 실현변동성)
```

변동성 큰 종목이 적게 들어가므로 **어느 한 종목도 결과를 지배할 수 없다.**

외부 증거: `OctopusTakopi/crypto-trend-following` 이 삭마 검사에서
**"효과가 확정된 것은 역변동성 사이징 하나뿐"**(위험목표·섹터 성분은 신뢰구간이
0 을 가로지름)이라고 보고했다.

## 이건 49 개 가설이 아니라 **하나**다

역변동성을 **전 49 팩터에 똑같이** 적용한다. 실패한 후보 하나를 구제하는 게
아니라, **집행 규칙 하나를 전면 적용**하는 것이다.

**주 결과는 짝비교다:**

> 49 개 팩터 각각에 대해 `Sharpe(역변동성) − Sharpe(동일가중)` 를 구하고
> **그 분포의 중앙값**을 본다. 이게 "역변동성이 엣지를 보존하는가"에 대한
> 단일 답이고, **검정 하나**다.

**그래도 49 건 전부 원장에 기록한다.** 짝비교가 검정 하나라는 주장은 옳지만,
개별 결과 중 최고를 나중에 후보로 쓰려면 그건 시행이다. 벌점을 미리 낸다.

## 신규 팩터 3 — 깃허브 사냥에서

| slug | 명세 | 출처 |
|---|---|---|
| `funding_z_90` | `(펀딩 − 90 일 평균) / 90 일 표준편차`, 낮은 쪽 롱 | `OctopusTakopi/funding-rate-alpha` |
| `riskadj_mom` | 30·90·180 일 `수익/변동성` 의 평균, 높은 쪽 롱 | 사냥 복합 모멘텀 |
| `ema_multi` | (16,48)·(32,96)·(64,192) EMA 차를 `가격×변동성` 으로 정규화해 평균, 높은 쪽 롱 | `OctopusTakopi/crypto-trend-following` |

`funding_z_90` 이 중요하다 — 우리 `funding_7`(+0.55)·`funding_30`(+0.21)은
**원 수준**을 썼는데, 저 저장소는 **자기 과거 대비 z 점수**를 썼고 그게 다른 신호다.

## 미리 선언하는 것

**1. 시행 수가 는다.** 49(역변동성) + 3(신규 동일가중) + 3(신규 역변동성) = **55 건**.
원장이 54 → 109 가 되고 잡음바닥이 또 오른다. **알고 한다.**

**2. 역변동성 창은 30 일로 고정.** 유동성 필터·분위와 같은 값이다.
결과를 보고 안 바꾼다.

**3. 짝비교가 주 결과다.** "역변동성으로 하니 X 가 통과했다"는 부차적이고,
그건 새 후보가 아니라 **새 시행**이다.

**4. 통과가 나와도 배터리 6 종을 다시 돌린다.** `#4` 와 같은 배터리다.

## 예상 — 틀려도 그대로 둔다

**짝비교 중앙값이 0 근처(−0.1 ~ +0.1)로 나올 것으로 본다.**

근거: 역변동성은 **변동성을 낮추고 수익도 같이 낮춘다.** Sharpe 는 비율이라
둘이 상쇄되는 게 보통이다. 낙폭은 확실히 줄겠지만 Sharpe 는 잘 안 움직인다.

**예외 예상: `taker_imb_30` 은 오히려 내려갈 것이다.** 그 엣지가 극단 종목에
얹혀 있었으므로, 극단 종목의 비중을 줄이면 엣지도 같이 준다.
**배터리가 이미 그 답을 줬다**(상위 10 제외 → 0.95).

**신규 3 중에서는 `funding_z_90` 이 가장 높을 것**으로 본다.
원 수준보다 z 점수가 낫다는 게 외부 저장소의 발견이고, 우리 원 수준 결과가
약했던 것과 일관된다. 다만 **1.10 을 넘을 것 같지는 않다** — 그 저장소도
2020~21 을 빼면 0.77 이었다.

## 반증 조건

- 짝비교 중앙값이 **+0.2 이상**이면 → 역변동성이 진짜 값을 더한다.
  전 카탈로그의 기본 집행으로 채택하고 재검정
- **−0.2 이하**면 → 역변동성이 엣지를 죽인다. 동일가중 유지
- **0 근처**면 → 집행 규칙으로는 못 푼다. `#4` 의 자산 집중 문제는
  **가중으로 해결되지 않는다**는 뜻이고, 다음은 **다른 데이터**(오픈인터레스트)다
