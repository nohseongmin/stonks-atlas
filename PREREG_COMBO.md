# 사전등록 — #2 시장중립 + 베타 조합

**작성 시각: 상관을 재기 전, 결과를 보기 전.** 커밋한 뒤에 실행한다.

## 왜

#1 에서 LTW L=14 가 **Sharpe 0.82 · 낙폭 34.9%**, 벤치 BTC 매수보유가
**0.90 · 낙폭 77%** 였다. 단독으로는 졌다. 그런데 낙폭 프로파일이 저렇게 다르면
**상관이 낮을 개연성**이 있고, 상관이 낮으면 조합이 둘 다를 이긴다.

```
조합 Sharpe = sqrt( (S1² + S2² − 2ρS1S2) / (1 − ρ²) )
```

| ρ | 조합 Sharpe | 절반켈리 월 |
|---:|---:|---:|
| +0.6 | 0.97 | 2.78% |
| +0.4 | 1.03 | 3.07% |
| +0.2 | 1.11 | 3.46% |
| 0.0 | 1.22 | 3.97% |

**월 5% 에 필요한 절반켈리 Sharpe 는 1.42.** 이 프로젝트에서 처음으로
목표 근처에 갈 수 있는 유일한 수학적 경로다.

**이건 #1 의 재검정이 아니다.** "LTW 가 BTC 를 이기는가"(기각됨)와
"둘을 섞으면 BTC 하나보다 나은가"는 다른 질문이다.

## 이 검정을 망칠 수 있는 유일한 방법 — 그래서 미리 막는다

**조합 비중을 사후에 최적화하면 그 숫자는 가짜다.** 최적 비중은 항상 존재하고,
그걸 찾아 보고하면 표본내 적합을 성과로 파는 것이다.

그래서 **비중 규칙을 지금 고정한다.** 두 개뿐이다.

| # | 규칙 | 정의 |
|---|---|---|
| 1 | **고정 50/50** | BTC 0.5 · LTW 0.5. 총 그로스 1.0 |
| 2 | **역변동성** | 직전 90 일 실현변동성의 역수에 비례. 총 그로스 1.0 |

역변동성은 **t 시점까지의 자료만** 쓴다. 90 일은 표준적인 값이고
결과를 보고 바꾸지 않는다.

## 미리 선언하는 것

**1. 상관이 얼마로 나오든 두 개 다 돌린다.** 상관을 먼저 재고 "높으니 관두자"
하면 그게 선택편향이다. **상관은 진단이지 게이트가 아니다.**

**2. 이건 2 건의 시행이다.** 원장 누적이 3 → 5 건이 된다. 둘 다 기록한다.

**3. 최적 비중을 계산하더라도 시행으로 보고하지 않는다.** 진단으로만 적고
**"이 숫자는 표본내 적합이라 배포 근거가 아니다"** 를 함께 적는다.

**4. LTW 다리는 L=14 하나만 쓴다.** #1 에서 셋 중 가장 좋았던 것이라
이미 선택이 한 번 들어갔다. **이 선택 자체가 다중검정 부담**이고,
잡음바닥 계산에 원장 5 건이 전부 들어가는 것으로 처리한다.

**5. 총 그로스를 1.0 으로 고정한다.** 켈리 배율을 여기서 최적화하지 않는다.
Sharpe 는 레버리지 불변이므로 관문 판정은 그로스와 무관하다.

**6. 관문은 `gate.py` 기본값 그대로.** 벤치는 여전히 BTC 매수보유다.
조합이 벤치를 **여유 +0.20 으로** 넘어야 하고 **비용 3 배에서도** 넘어야 한다.

## 예상 — 틀려도 그대로 둔다

**지금까지와 달리, 이번엔 통과 가능성이 실제로 있다고 본다.**

근거: LTW 는 구성상 시장중립이다(가중치 합 = 0). 완전한 무상관은 아닐 것이다 —
크립토 횡단면 모멘텀의 승자는 고베타 쪽으로 쏠리는 경향이 있어서 잔여 롱베타가
남는다. 그래서 **ρ = 0.1 ~ 0.4** 로 본다.

**구체적으로: 조합 Sharpe 1.00 ~ 1.15.**
벤치 0.90 대비 여유 +0.10 ~ +0.25 이므로 **④ 벤치대비는 걸릴 수도 통과할 수도 있다.**
⑤ 비용 3 배가 더 위험하다 — LTW 다리의 Sharpe 가 0.82 → 0.63 으로 떨어지므로
조합도 같이 내려간다.

**최종 예상: 고정 50/50 은 ④ 를 아슬아슬하게 통과하고 ⑤ 에서 기각.**
역변동성은 BTC 변동성이 LTW 보다 높아 LTW 쪽에 비중이 실릴 것이고,
그러면 LTW 단독에 가까워져 **④ 에서 기각**.

## 반증 조건

- 조합이 **관문 7 종을 전부 통과**하면 → 이 프로젝트 486 시행 중 **첫 통과**다.
  그러면 즉시 배포하지 않고 **하위표본 분할 + 전진검증**으로 넘긴다.
  한 번 통과했다고 배포하는 것이 이 프로젝트가 하지 않기로 한 일이다
- ④ 는 통과하는데 ⑤ 에서 죽으면 → 조합의 값이 **비용 가정에 얹혀 있다.**
  실전 배포 불가. 다만 저비용 거래소(Hyperliquid 9.0bp)에서 재측정할 값은 있다
- 둘 다 벤치 미달이면 → **상관이 높았다는 뜻**이고, LTW 는 시장중립이라는
  이름과 달리 실제로는 베타였다는 것이다. 그러면 남는 건 B(He et al. 무기한 차익) 하나
